spring.application.name=20_spring_security

#We can define only one user with this way, not more than that
#We can change the default credentials

#spring.security.user.name =Viraj
#spring.security.user.password =123